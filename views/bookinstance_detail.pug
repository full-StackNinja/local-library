extends layout

block content
  h1 ID: #{bookInstance._id}
  p #[strong Title: ]
    a(href=bookInstance.book.url) #{bookInstance.book.title} 
  p #[strong Imprint: ] #{bookInstance.imprint}
  p #[strong Status: ] &nbsp;
    if bookInstance.status === 'Available'  
      span.available #{bookInstance.status}
    else if bookInstance.status === 'Maintenance'
      span.maintenance #{bookInstance.status}
    else 
      span.loaned #{bookInstance.status}
  if bookInstance.status !=='Available'
    p #[strong Due back:] #{bookInstance.due_back_formatted}
  
  hr 
  p
    a(href=bookInstance.url+'/delete') Delete this copy
  
  p
    a(href=bookInstance.url + '/update') Update this copy

