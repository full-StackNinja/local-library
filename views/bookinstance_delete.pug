extends layout

block content

  h1 ID: #{bookInstance._id}
  p #[strong Title: ]
    a(href=bookInstance.book.url) #{bookInstance.book.title}
  p(style='margin:50px 0;') Do you want to delete this copy?
  form(method='POST')
    button.btn.btn-primary(type='submit') Submit